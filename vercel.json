{
    "installCommand": "uv sync --extra api",
    "builds": [
        {
            "src": "src/persona/api/app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/persona/api/app.py"
        }
    ]
}
